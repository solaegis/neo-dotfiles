#!/bin/sh
# Install Homebrew formulae/casks from Brewfile when brew data changes.
# brew-checksum: {{ .brew | toJson }}
set -e
if command -v brew >/dev/null 2>&1 && [ -f "$HOME/Brewfile" ]; then
  brew bundle install --file="$HOME/Brewfile"
fi
