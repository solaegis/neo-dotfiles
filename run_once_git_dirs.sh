#!/bin/sh
# Create per-identity git directories. Repos under ~/git/IDENTITY use the matching SSH key via includeIf.
mkdir -p "$HOME/git/acxiom" "$HOME/git/solaegis" "$HOME/git/dadandlad" "$HOME/git/nuvent"
